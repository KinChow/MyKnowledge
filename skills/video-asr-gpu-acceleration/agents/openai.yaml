interface:
  display_name: "Video ASR GPU Acceleration"
  short_description: "Archive video transcripts with remote GPU acceleration"
  default_prompt: "Use $video-asr-gpu-acceleration to inventory, download, transfer, transcribe, validate, and archive this video-to-text task."
