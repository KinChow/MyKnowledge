/archive/raw/** filter=lfs diff=lfs merge=lfs -text
# A3 原件保留：content/sources/<domain>/<id>/<id>.<ext> 二进制原件走 LFS，
# 与 archive/raw 归档一致（文本 .md 不匹配此规则，正常入库）。
# 后续新增视频/音频等原件格式时，在此追加对应扩展名。
/content/sources/**/*.pdf filter=lfs diff=lfs merge=lfs -text
