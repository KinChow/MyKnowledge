import fs from 'node:fs'; import {execFileSync} from 'node:child_process'; const old=fs.existsSync('dist')?fs.cpSync('dist','dist.previous',{recursive:true}):null; try { execFileSync('node',['scripts/prepare-content.mjs'],{stdio:'inherit'}); execFileSync('node',['scripts/build-graph.mjs'],{stdio:'inherit'}); execFileSync('npx',['astro','build','--outDir','dist.next'],{stdio:'inherit'}); execFileSync('node',['scripts/validate-build.mjs'],{stdio:'inherit'}); execFileSync('node',['scripts/leak-gate.mjs','dist.next'],{stdio:'inherit'}); fs.rmSync('dist',{recursive:true,force:true}); fs.renameSync('dist.next','dist'); } catch(e) { if(fs.existsSync('dist.next')) fs.rmSync('dist.next',{recursive:true,force:true}); if(fs.existsSync('dist.previous')) {fs.rmSync('dist',{recursive:true,force:true}); fs.renameSync('dist.previous','dist');} process.exit(1); }
