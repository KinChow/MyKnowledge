import fs from 'node:fs'; const catalog=JSON.parse(fs.readFileSync('public/generated/catalog.json','utf8')); const ids=new Set(catalog.items.map(x=>x.id)); const nodes=catalog.items.map(x=>({id:x.id,title:x.title})); const edges=[]; for(const item of catalog.items) for(const target of item.links||[]) if(ids.has(target)) edges.push({source:item.id,target}); fs.writeFileSync('public/generated/graph.json',JSON.stringify({schema_version:'graph/v1',generated_from:catalog.generated_from,nodes,edges},null,2));
