import fs from 'node:fs'; import path from 'node:path'; const targets=process.argv.slice(2); const pathLeak=/(^|\/)(sources|archive|practice|state|queries\/local)(\/|$)/i; const contentLeak=/confidentiality:\s*internal|\bprivate\b|javascript:|<script[^>]*src=.*file:/i; const contentExt=new Set(['.html','.htm','.json','.md','.css','.txt','.xml','.yaml','.yml']); const findings=[]; function walk(p){if(!fs.existsSync(p))return; const st=fs.statSync(p); if(st.isDirectory()) for(const n of fs.readdirSync(p)) walk(path.join(p,n)); else {const rel=path.relative(process.cwd(),p); if(pathLeak.test(rel)){findings.push(rel); return;} if(contentExt.has(path.extname(p).toLowerCase())) {const text=fs.readFileSync(p,'utf8'); if(contentLeak.test(text)) findings.push(rel);}}} for(const p of targets) walk(p); if(findings.length){console.error(JSON.stringify({schema_version:'public-leak-gate/v1',findings})); process.exit(2);} console.log(JSON.stringify({schema_version:'public-leak-gate/v1',findings:[]}));
