---
aliases: []
confidentiality: public
domain: computer-science
evidence:
- claim: '设计模式

    设计模式（Design pattern）代表了最佳的实践，通常被有经验的面向对象的软件开发人员所采用。设计模式是软件开发人员在软件开发过程中面临的一般问题的解决方案。这些解决方案是众多软件开发人员经过相当长的一段时间的试验和错误总结出来的。

    本教程将通过 Java 实例，一步一步向您讲解设计模式的概念。

    谁适合阅读本教程？

    无论您是新手，还是老手，本教程都值得一读。对于那些具有丰富的开发经验的开发人员，学习设计模式有助于了解在软件开发过程中所面临的问题的最佳解决方案；对于那些经验不足的开发人员，学习设计模式有助于通过一种简单快捷的方式来学习软件设计。

    阅读本教程前，您需要了解的知识：

    在您开始阅读本教程之前，您必须具备基本的 Java 编程的概念。如果您还不了解这些概念，那么建议您先阅读我们的 Java 教程。'
  claim_id: solid-audit-1
  support: direct
  supporting_quotes:
  - evidence_id: evidence-26c0d56f8f5a
    exact: '设计模式

      设计模式（Design pattern）代表了最佳的实践，通常被有经验的面向对象的软件开发人员所采用。设计模式是软件开发人员在软件开发过程中面临的一般问题的解决方案。这些解决方案是众多软件开发人员经过相当长的一段时间的试验和错误总结出来的。

      本教程将通过 Java 实例，一步一步向您讲解设计模式的概念。

      谁适合阅读本教程？

      无论您是新手，还是老手，本教程都值得一读。对于那些具有丰富的开发经验的开发人员，学习设计模式有助于了解在软件开发过程中所面临的问题的最佳解决方案；对于那些经验不足的开发人员，学习设计模式有助于通过一种简单快捷的方式来学习软件设计。

      阅读本教程前，您需要了解的知识：

      在您开始阅读本教程之前，您必须具备基本的 Java 编程的概念。如果您还不了解这些概念，那么建议您先阅读我们的 Java 教程。'
  targets:
  - evidence_id: evidence-26c0d56f8f5a
    source_id: web-computer-science-software-design
id: solid
kind: knowledge
publication_scope: public
related: []
schema_version: wiki/v1
sources:
- web-computer-science-software-design
- working-computer-science-solid
status: published
tags:
- solid
- design-principle
- oop
- design-pattern
title: SOLID原则
updated_at: '2026-09-06'
---
# SOLID原则

## 一句话结论

SOLID 是面向对象设计的一组核心原则，包含五个字母对应的原则：**单一职责（SRP）、开闭原则（OCP）、里氏替换原则（LSP）、接口分离原则（ISP）、依赖倒置原则（DIP）**。它们共同指导软件实体做到"高内聚低耦合"——让模块面向扩展而非修改、让依赖面向抽象而非细节，从而提升代码的可维护性与可扩展性。

## 核心概念

- **单一职责（The Single Responsibility Principle）**：一个类引起其变化的原因只有一个；一个对象应该只包含单一的职责，并且该职责被完整地封装在一个类中。
- **开闭原则（The Open Closed Principle）**：对扩展开放，对修改关闭；模块可以在不被修改的前提下进行扩展，即在不修改源代码的情况下改变模块的行为。
- **里氏替换原则（The Liskov Substitution Principle）**：基类设定一系列规范和契约，子类需要遵守；任何基类可以出现的地方，子类也一定可以出现。
- **接口分离原则（The Interface Segregation Principle）**：类不应该被迫依赖他们不使用的方法；一个接口应该拥有尽可能少的行为，它是精简的、单一的。
- **依赖倒置原则（The Dependency Inversion Principle）**：高层模块不应该依赖低层模块，它们都应该依赖抽象；抽象不应该依赖于细节，细节应该依赖于抽象。

## 工作机制

五条原则分别从不同角度控制"变化"的影响范围：

| 原则 | 核心思想 | 解决的问题 |
| --- | --- | --- |
| 单一职责 SRP | 一个类引起其变化的原因只有一个 | 一个变化导致多处修改（存在重复） |
| 开闭原则 OCP | 对扩展开放，对修改关闭 | 多个变化导致一处修改、对变化识别不准、修改易引入问题 |
| 里氏替换 LSP | 子类可替换基类，遵守基类规范契约 | 继承关系破坏基类约定导致替换错误 |
| 接口分离 ISP | 接口拥有尽可能少的行为，精简单一 | 类被迫依赖不使用的（臃肿）方法 |
| 依赖倒置 DIP | 双方依赖抽象，不依赖具体实现 | 高层依赖低层细节导致变更传导 |

- **SRP** 通过"单一职责、完整封装"消除"一个变化多处修改"的重复。
- **OCP** 通过"对扩展开放、对修改关闭"让模块在不改源代码的前提下改变行为。
- **LSP** 约束继承——基类设定规范契约，子类遵守，保证多态替换的正确性。
- **ISP** 通过"精简、单一的接口"避免客户端依赖不需要的行为。
- **DIP** 让"接口为稳定的契约，双方都依赖于抽象"——上层定义接口，下层实现接口。

## 示例或代码

- **单一职责**：把"报表生成"与"数据持久化"拆到不同类，各自只有一个变化原因；改报表格式不动持久化逻辑。
- **开闭原则**：为支付方式定义抽象 `Payment` 接口，新增支付方式时新增实现类（扩展），不改动业务调用代码（关闭）。
- **里氏替换**：`Square`（正方形）作为 `Rectangle`（矩形）的子类时，若重写宽度/高度导致面积约定被破坏，则违反 LSP——子类必须能安全替换基类出现的位置。
- **接口分离**：把"打印机"拆成 `Printer`（打印）与 `Scanner`（扫描）两个小接口，而不是一个包含 `scan()` 的臃肿接口，避免只支持打印的类被迫实现扫描方法。
- **依赖倒置**：高层业务依赖 `IDataSource` 抽象接口，低层实现（如 `MySqlDataSource`）实现该接口；上层定义接口、下层实现接口。

## 常见误区

- **把单一职责理解为"一个类只做一件事"**：准确表述是"引起变化的原因只有一个"，职责以"变化原因"界定，而不是以行为数量界定。
- **把开闭原则理解为"完全不修改"**：它针对的是"模块的行为改变"方式——通过扩展而非修改源码实现；识别变化并预留扩展点是前提。
- **忽视里氏替换**：继承时只关注复用而破坏基类契约，会导致多态替换后行为异常（经典反例：正方形/矩形）。
- **把依赖倒置等同于"面向接口"**：DIP 强调"上层定义接口、下层实现接口、双方依赖抽象"，不只是简单地使用接口关键字。
- **把 SOLID 当作银弹**：SOLID 是设计指导原则，不是强制规范，需结合场景权衡（过度设计也违背 YAGNI）。

## 证据映射

| Claim | 来源 | 要点 |
| --- | --- | --- |
| solid-audit-1 | web-computer-science-software-design | 设计模式（含设计原则）代表最佳实践，是解决一般软件问题的方案，由众多开发者长期试验总结而来 |

## 待验证项

无。

## 关联知识

- [[software-design]] —— 软件设计总览：设计原则集合（含 SOLID）。
- [[orthogonal-design-principles]] —— 正交设计四原则。
- [[object-oriented]] —— 面向对象特性与设计思想。
- [[four-rules-of-simple-design]] —— 简单设计四原则。
- [[lod]] —— 迪米特法则（最少知识原则）。
- [[common-performance-design-patterns]] —— 设计模式与性能设计模式。
- [[refactoring-concepts-and-principles]] —— 重构：应用设计原则的实践。

## 详细章节

### SOLID原则

#### 单一职责（The Single Responsibility Principle）

一个类引起其变化的原因只有一个。如果一个变化，导致多处修改，则存在重复

一个对象应该只包含单一的职责，并且该职责被完整地封装在一个类中



#### 开闭原则（The Open Closed Principle）

对扩展开放，对修改关闭。多个变化导致一处修改，则对变化的识别不准，容易考虑不周，修改引入

一个软件实体应当对扩展开放，对修改关闭。也就是说在设计一个模块的时候，应当使这个模块可以在不被修改的前提下进行扩展，即实现在不修改源代码的情况下改变这个模块的行为



#### 里氏替换原则（The Liskov Substitution Principle）

基类设定一系列的规范和契约，子类需要遵守

派生的子类应该是可替换基类的，即任何基类可以出现的地方，子类也一定可以出现



#### 接口分离原则（The Interface Segregation Principle）

客户只需要了解必须了解的，不需要暴露细节

类不应该被迫依赖他们不使用的方法，即一个接口应该拥有尽可能少的行为，它是精简的，单一的



#### 依赖倒置原则（The Dependency Inversion Principle）

接口为稳定的契约，双方都依赖于抽象，不依赖于具体实现；上层定义接口，下层实现接口

高层模块不应该依赖低层模块，它们都应该依赖抽象。抽象不应该依赖于细节，细节应该依赖于抽象
