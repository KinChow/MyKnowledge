# ADR-0006：Preview/Apply 写协议

- 状态：Accepted
- 日期：2026-08-25
- 相关规范：OPS
- 相关 Feature：F004

## 决策

所有写入必须经过 Preview、用户确认和 Apply。Apply 使用幂等键、仓库级写锁和原子落盘；rename、move、废弃和删除是受控的一等操作。

## 后果

写入更可审计，Agent 不能绕过门禁直接修改文件；实现需要处理重试、中断和恢复。

## 重新评估条件

出现新的写入客户端或需要跨仓库事务时重新评估。
